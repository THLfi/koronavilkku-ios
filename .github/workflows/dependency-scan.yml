name: Check dependencies

on:
  push:
    branches: '*'
  schedule:
  - cron:  "0 5 * * *" # this is utc time

jobs:
  dependency-scan:
    name: Owasp Dependency Scanning
    runs-on: macOS-latest
    steps:
      - uses: actions/checkout@v2
      - name: Cache Derived data and vulnerability data cache
        uses: actions/cache@v2
        with:
          path: |
            DerivedData
            /owasp/dependency-check/data
          key: ${{ runner.os }}-owasp-dependency-scan-${{ hashFiles('**/*.pbxproj') }}
          restore-keys: ${{ runner.os }}-owasp-dependency-scan
      - name: Build app
        run: xcodebuild build -workspace Koronavilkku.xcworkspace -scheme Koronavilkku -derivedDataPath DerivedData CODE_SIGNING_REQUIRED="NO"
      - name: OWASP dependency check
        uses: ./.github/docker-owasp-dependency-check-ios
        with:
          derived-data-path: DerivedData
      - uses: actions/upload-artifact@v2
        if: ${{ success() || failure() }}
        with:
          name: dependency-check-result
          path: ./**/build/reports/dependency-check-report.html
      - name: Notify to slack
        if: ${{ failure() }}
        uses: ./.github/docker-slack
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        with:
          text: "OWASP dependency check"
          status: ${{job.status}}
      - name: Notify by email
        if: ${{ failure() }}
        uses: ./.github/docker-notify-email
        env:
          API_KEY: ${{ secrets.EMAIL_SERVICE_API_KEY }}
          SEND_URL: ${{ secrets.EMAIL_SERVICE_SEND_URL }}
          RECIPIENTS: ${{ secrets.EMAIL_SERVICE_RECIPIENTS }}
          FROM: ${{ secrets.EMAIL_SERVICE_SENDER }}
        with:
          status: ${{job.status}}
